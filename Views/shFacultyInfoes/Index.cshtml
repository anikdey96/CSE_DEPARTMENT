@model IEnumerable<CSE_DEPARTMENT.Models.shFacultyInfo>

@{ ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_HomeLayoutPage.cshtml"; }



<br />
<br />
<br />
<br />
<br />
<br />

<div class="card shadow mb-4">
    <div class="card-header py-3">
        <h3 class="m-0 font-weight-bold text-primary">Apply For Leave</h3>

    </div>

    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-condensed" id="dataTable" width="100%" cellspacing="0">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Designation)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Name)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.TotalLeave)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.LeaveTaken)
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Designation)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.TotalLeave)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.LeaveTaken)
                        </td>
                        @*<td class="btn-group">
                                @if (User.Identity.IsAuthenticated && (User.IsInRole("SuperAdmin")))
                                {
                                    @Html.ActionLink("Edit", "Edit", new { id = item.application_id }, new { @class = "btn btn-primary" })
                                    @Html.ActionLink("Delete", "Delete", new { id = item.application_id }, new { @class = "btn btn-danger" })
                                }
                            </td>*@
                    </tr>
                }

                </tbody>
            </table>

        </div>

    </div>

</div>

<p>
    @if (User.Identity.IsAuthenticated && (User.IsInRole("faculty2")))
    {
        @Html.ActionLink("Create A New Application", "Create", "shaFacultyApplications", null, new { @class = "btn btn-success" })
    }

</p>